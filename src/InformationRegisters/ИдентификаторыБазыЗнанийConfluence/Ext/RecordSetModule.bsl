
Перем УдалитьКонтент Экспорт;

Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ЭтотОбъект.Количество() = 0 И ЭтотОбъект.Модифицированность() = Истина Тогда
		Если УдалитьКонтент <> Истина Тогда
			Отказ = Истина;
			ВызватьИсключение НСтр("ru='Удаление идентификаторов допускается только командой ""Удалить контент"".'");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

УдалитьКонтент = Ложь;
